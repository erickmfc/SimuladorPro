import React from "react";
import { Shield, Trophy, History, LayoutGrid, ChevronRight } from "lucide-react";
import { cn } from "../lib/utils";
import { CompetitionType } from "../services/footballService";

export const CompetitionSelector: React.FC<any> = () => {
  return null; // Integrated into App.tsx sidebar
};
